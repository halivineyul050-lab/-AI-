# 项目冗余文件检查

检查日期：2026-09-15。范围：本地工作区文件、服务入口、静态资源白名单、引用、配置路径、部署脚本和历史文档。随后按本报告的“优先清理候选”执行了本地清理，未连接生产环境，未执行部署脚本。未运行功能测试；这是静态清理审计，不代表全站功能验收。

## 总体结论

- 检查时共有 480 个文件，约 410.05 MiB（不含 `.git`）；其中 `tmp/` 94 个文件、369.45 MiB，占约 90%。大小是文件逻辑大小，实际磁盘释放量可能不同。
- `tmp/` 不能整目录直接删除：本地 `.env` 的图片背景和增强模型配置仍指向 `tmp/image-suite/` 中现存的两个 ONNX 模型。
- 工作区有大量未跟踪的新功能和已有修改，未跟踪不等于无用。不要用 `git clean -fd` 一把清空。
- 主站是 Node.js + SQLite，原生 HTML/CSS/JS；游戏和图片/视频工具已注册路由，属于实际功能。

## 1. 优先清理候选

下列文件不属于当前应用运行入口或正式资源。清理测试数据库、日志之前应先停止对应本地预览/写入进程；历史发布脚本如还需复用应先归档。

| 路径 | 大小 | 内容与依据 | 建议 |
|---|---:|---|---|
| `tmp/crop-review/` | 81.73 MiB | 裁剪样片、结果视频、FFmpeg 下载包和解包目录、上游提交记录 | 可清理；正式 FFmpeg 位于 `assets/vendor/ffmpeg/` |
| `tmp/link-review/` | 54.59 MiB | `douyin-audio.mp3`、`test-audio.mp3`，链接提取测试下载结果 | 无需保留测试样本时可删除 |
| `tmp/tools-20260908/` | 12.32 MiB | 旧发布包、校验值、基线源码副本和一次性修补/部署脚本 | 归档有用脚本后清理；不能用它覆盖当前源码 |
| `tmp/pdfs/` | 4.26 MiB | 报告逐页渲染预览图 | 可删除，不是报告源稿 |
| `tmp/gif-review/` | 2.79 MiB | 视频/GIF 测试结果、预览 SQLite 数据库及 WAL/SHM | 停止相关预览后整体清理 |
| `tmp/mask-review/` | 0.41 MiB | `blur.mp4`、`mosaic.mp4`、`invalid.mp4` 测试素材 | 无需保留样本时可删除 |
| `tmp-home.png`、`tmp-aibot-home.png` | 合计 0.88 MiB | 临时页面截图，应用代码未发现引用 | 可删除 |
| `scripts/__pycache__/` | 约 17 KiB | 两个 Python 自动生成的 `.pyc` | 可删除，运行时会重新生成 |
| `.agents/*.png` | 2.25 MiB | 首页、移动端及排行榜的五张开发截图 | 不再需要视觉验收记录时可删除；不要推广为删除所有 agent 配置 |
| `data/server-error.log` | 0 B | 空日志文件 | 可清理，保留也不占文件内容空间 |
| `data/server-output.log`、`data/server.log` | 合计约 74 KiB | 本地运行日志 | 无排障保留需求且停止写入后可清理 |

重复文件已通过 SHA-256 核对：以下三个文件内容完全一致，保留第一个，后两个属于临时副本，合计约 61.47 MiB，已包含在上表 `crop-review` 中，不重复计算。

```text
assets/vendor/ffmpeg/ffmpeg-core.wasm
tmp/crop-review/package/dist/esm/ffmpeg-core.wasm
tmp/crop-review/package/dist/umd/ffmpeg-core.wasm
SHA-256: 9F57947A5BD530D8F00C5B3F2CB2A3492FAA7E5D823315342D6A8656D0A6B7B7
```

## 2. 大文件需要先处理依赖

### `tmp/image-suite/`：6.75 MiB

必须暂时保留：

- `u2netp.onnx`：4,574,861 字节。
- `super-resolution-10.onnx`：240,078 字节。

本地 `.env` 的 `NIKE_IMAGE_BACKGROUND_MODEL` 和 `NIKE_IMAGE_ENHANCE_MODEL` 分别引用它们，且两个路径均存在。`backend/image-tools.mjs:35` 读取这些配置。建议以后迁至专用模型目录，更新配置并验证图片功能后，才能清理原路径。

其余是截图、测试日志、发布包、部署验证脚本等，约 2.16 MiB，可在确认不需复用后清理。生产部署文档记载的模型位置是 `/opt/nikai-image-tools/`，这只是文档证据，本次未核验服务器实际状态。

### `tmp/image-erase/`：206.60 MiB

- `verified-big-lama.pt`：196.14 MiB，是最大的单文件。运行模块通过 `NIKE_ERASE_MODEL` 接收路径，模型本身不是垃圾；本次未发现本地 `.env` 对它的引用。若需保留本地消除功能模型，迁至模型目录；若只作为历史下载缓存且已有可用副本，可删除。
- `big-lama.pt`：5.23 MiB；`sanster-big-lama.pt`：2.19 MiB。明显小于 verified 文件，但仅凭大小不能认定损坏或重复；未做模型加载验证。确认是失败下载/弃用模型后再删。
- `preview.db`、`preview.db-wal`、`preview.db-shm`：预览数据库，停止预览后成组处理，不能单独删 WAL。
- 其余为旧发布包、截图、诊断/部署脚本、HTTP 检查记录等；无历史追溯需求时清理。

### `tmp/build-tools-release.mjs`

硬编码 `tools-20260908` 发布号，按当时功能名称选择文件；它不是当前完整发布清单。可归档为历史脚本，不宜直接作为当前发版入口。

## 3. 与运行无关，但应归档而非默认丢弃

| 路径 | 内容 | 建议 |
|---|---|---|
| `C盘清理扫描/`（11 个文件，约 0.10 MiB） | 独立电脑磁盘分析任务：8 份 CSV、2 个生成/扫描脚本、1 份 Word 报告 | 移出网站项目。属于用户资料，不按缓存直接删除 |
| `funai-design-cases/`（5 个 HTML，约 0.05 MiB） | 设计方向首页和四个设计样例 | 主服务未注册这些页面，应用侧未发现引用；不用作设计参考即可归档或删除 |
| `output/pdf/`（3 个文件，约 0.10 MiB） | 两份竞品 PDF 和一份 HTML 成稿 | 网站运行不依赖；属于交付成果，优先移到报告资料目录 |
| `泥壳AI工具站_竞品横纵分析报告.md` | 竞品分析源稿 | 与对应 PDF 一并归档；报告生成脚本会引用此源稿 |
| `AI工具集_ai-bot.cn_横纵分析报告.md` | 竞品分析源稿 | 移入研究资料目录 |
| `ai-bot-cn-深度解析与独立平台开发方案-2026-07-13.md` | 早期研究和开发方案 | 归档，不作为当前实现说明 |
| `scripts/build_competitor_report_pdf.py` | 将竞品 Markdown 生成 PDF 的脚本 | 不是服务启动依赖；如保留报告再生成能力，应连同源稿一起迁移并改路径 |
| `AI工具批量导入与合规说明-2026-07-14.md`、`AI工具与资讯补充记录-2026-07-13.md` | 历史导入说明和操作记录 | 保留数据来源价值，移入历史文档 |
| `imports/ai-comic-official-20260717*.json` | 两批历史导入输入 | 确认已导入并保留追溯副本后归档；保留模板 CSV |
| `.workbuddy/2026-07-31.md` | 辅助工具历史记录 | 无运行依赖；是否保留取决于后续是否使用该工具 |
| `brand-icon.svg`（约 0.59 MiB） | 品牌图形源文件 | 当前运行代码未引用，仅 README 引用；可移到设计源文件目录，不宜当无价值文件删除 |
| `scripts/resolve-novatools-catalog.mjs` | 手工执行的目录转换/解析工具 | 未发现 npm 命令或其他文件调用；独立 CLI 可能仍有用途，停用后再归档 |

## 4. 文件内过时或重复的内容

1. `上线前检查清单.md` 的“后台使用共享 Bearer Token”“邮件订阅目前只入库”已经不能概括当前代码。现有账号、会话、超级管理员权限代码，也有 Resend 邮件发送实现和周报脚本。应更新这些段落，保留仍适用的上线检查。邮件实现存在不代表线上已配置或成功送达，也不能据此声称 MFA 已实现。
2. `backend/README.md:85`、`泥壳AI工具站-后端地址与接口说明.md:138` 的“尚未接入邮件发送”应更新。
3. `泥壳AI工具站-后端地址与接口说明.md:212` 的“没有管理员账户、MFA 和 RBAC”混合了已实现和未核实能力，应逐项重写；`backend/README.md:138` 和后续生产建议也需同步。
4. `昨日任务整理与未完成清单-2026-08-26.md` 是历史快照，47/47 测试等数字不能视作当前测试结果。但其中自动教程草稿列出的五个待实现文件目前仍不存在，不能把整份清单当作失效内容删除。
5. `docs/superpowers/` 中已完成游戏/图片/视频功能的 plans/specs 可归档并标记状态；自动教程草稿计划仍有待办价值。
6. `docs/deployment-*.md` 是发布追溯资料，建议保留并与最新运行说明分开，不必删除。

## 5. 明确保留

- `data/nike-ai.db` 及其 WAL/SHM：应用数据；不是可以随意重建的缓存。
- `.env`、`.env.example`、`.git/`：本地配置、配置模板和版本历史。
- `backend/schema.sql`、全部 migrations、`backend/seed-data.json`：数据库模块及启动代码明确读取，不能因已有数据库就删除。
- 游戏、图片、视频、链接提取页面及 JS/CSS/core/worker：`server.mjs:110` 静态白名单及 `server.mjs:360` 路由表实际注册。
- `assets/vendor/ffmpeg/`、`assets/vendor/gifenc/` 和许可证/NOTICE：实际功能依赖及来源说明。
- `assets/tool-logos/`：逐文件名检查后，未发现完全无引用的 Logo；目录清单/测试中的引用不等于每张都在当前数据库展示，本次不作进一步删除判断。
- `tests/`：不仅供本地开发，`scripts/release-production.sh` 也执行 `npm test`，不能从现有发布流程随意剔除。
- `deploy/`、`.github/`、正式发布/备份/回滚脚本、导入脚本：运行维护和发布所需。
- `seo-analytics.js`：服务生成的多类 HTML 明确引用。当前静态白名单却未包含它，这是需要另行验证/修复的资源服务问题，不是删除依据。
- 站点验证 HTML、robots、sitemap、manifest 和品牌 PNG：SEO、站点验证及图标资源。

## 6. 防止再次混入

当前 `.gitignore` 没有排除 `tmp/`、`__pycache__/`、`*.pyc`、根目录临时截图，也没有针对报告产物和独立磁盘扫描资料的规则。建议整理后补上明确规则，不要笼统忽略所有图片、模型或文档。

`.github/workflows/deploy.yml:34` 只排除了 `.git/.env/data/node_modules/imports`。已提交的设计样例、研究资料等仍会随打包进入发布包；未跟踪的本地临时文件不会凭空出现在 Actions checkout，但未来误提交后也会被打包。建议明确发布范围；现有发布脚本要求 tests，制定范围时必须保留。

优先顺序：清理已确认重复的 FFmpeg 临时副本和测试下载 → 迁移两个被配置引用的 ONNX 模型 → 分类处理消除模型和预览数据库 → 归档研究/设计/电脑清理资料 → 更新过时文档和忽略/打包规则。
