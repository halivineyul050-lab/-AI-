# 泥壳 AI 工具站上线与验收清单

本文描述当前项目的本地验证与线上发布检查。生产架构为单机 Node.js + MariaDB 10.5 + Nginx；本地开发和自动化测试默认使用 SQLite。不要照搬旧版本中的 `NIKE_DB_PATH` SQLite 生产配置或 PostgreSQL 迁移建议。

## 发布前

- 确认目标代码来自预期分支和提交，检查 `git status`、`git log`，不要把未审阅的工作区文件混入发布包。
- 在 Node.js 22.5+ 环境运行：

```powershell
npm test
npm run logos:verify
```

- 检查新增数据库迁移、环境变量和静态资源是否随发布包包含。
- 确认 GitHub Actions 所需 SSH secrets 已配置；密钥、`.env`、数据库和备份均不得加入仓库或发布包。
- 生产服务器 `.env` 使用权限受限文件保存 MariaDB 凭据、`NIKE_ADMIN_TOKEN`、稳定的 `NIKE_ANALYTICS_SALT` 和 `NIKE_IMAGE_CONFIG_KEY`。图片主密钥是 64 位十六进制字符串，须独立于数据库备份安全托管。

## 生产发布

按 [生产发布流程](docs/production-release.md) 走 GitHub Actions `main` 发布或受控 SSH 发布。发布脚本应先按 `NIKAI_DB_ENGINE` 选择数据库备份，再保存代码快照、安装依赖、运行测试、重启服务并检查健康状态。

在 MariaDB 备份脚本可用的前提下，发布前确认产生了新备份；若仍在使用旧发布脚本，它可能只调用 SQLite 备份，必须先停下并修复/更新发布脚本，不能把旧 SQLite 文件当成生产备份。代码回滚与数据库恢复是两项独立操作；运行过的新迁移不会因为代码回滚自动撤销。

## 上线后检查

访问线上域名确认以下端点为 HTTP 200，健康数据中的状态为 `ok`：

```text
/api/v1/health/live
/api/v1/health/ready
/api/v1/site/bootstrap
/api/v1/site-announcements
```

再实际检查首页、资讯、工具详情、官网跳转、投稿入口、公告弹窗、图片生成页面、后台登录和运营后台列表。检查管理操作需要认证，未认证请求不得返回管理数据。不要在浏览器、日志或工单中粘贴生产令牌和平台 API Key。

## 数据和密钥恢复

- 定期备份 MariaDB，并异地保存；定期做隔离环境恢复演练。
- `NIKE_IMAGE_CONFIG_KEY` 与数据库备份分开托管。若仅有密文备份而主密钥丢失，图片平台 API Key 无法解密，恢复后必须重新录入。
- 监控服务健康、MariaDB、磁盘空间、备份新鲜度和 5xx 错误。
- 生产公告、文章及其他 CMS 内容保存在数据库；不要用本地种子快照覆盖线上数据库。

## 当前范围与未完成事项

- 生产采用 MariaDB，不需要为当前发布迁移到 PostgreSQL。
- 周报订阅目前记录在库，尚未接入正式邮件发送服务。
- `.superpowers/` 中的发布候选、恢复快照及其他工作资料尚未清理；执行不可逆清理前先由项目所有者逐项确认。
